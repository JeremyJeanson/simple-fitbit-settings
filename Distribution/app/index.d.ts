/**
 * Initialize settings
 * @param settings that will be managed by the module
 * @param callback to receive changin settings
 */
export declare function initialize<T>(settings: T, callback: (newSettings: T) => void): void;
